# Opcional. Em dev local não precisa — o proxy do Vite já encaminha /api pro
# server (vite.config.ts). Só defina se client e server forem hospedados em
# domínios separados na Vercel (aponte pra URL pública do projeto do server).
VITE_API_URL=
